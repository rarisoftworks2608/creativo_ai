# Core web framework
Django==5.0.6                  # Main Django framework for backend/API server

# REST API support
djangorestframework==3.15.1    # Adds tools to build REST APIs (serializers, viewsets, etc.)

# Cross-Origin Resource Sharing
django-cors-headers==4.3.1     # Allows React (different port/domain) to call Django APIs

# Environment variables
python-dotenv==1.0.1           # Load secrets/config (SECRET_KEY, DB credentials) from a .env file
